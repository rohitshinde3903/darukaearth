/** @type {import('next').NextConfig} */
const nextConfig = {
  turbopack: {
    // Empty turbopack config to silence the warning
    // Turbopack handles mapbox-gl correctly by default
  },
};

module.exports = nextConfig;
